# GitHub Pages deployment disabled — repo is private, Vercel handles deployment.
# This workflow is kept for reference only.

name: Deploy to GitHub Pages (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: echo "Deployment is handled by Vercel. This workflow is disabled."
