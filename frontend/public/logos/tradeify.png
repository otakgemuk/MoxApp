These files are placeholders. The GitHub API connector base64-encodes text
content, so binary PNGs cannot be committed through it (they arrive double-
encoded and render as text). Host the MightyOx email logos on GetResponse's
image manager instead (same CDN as the main logo), then paste those URLs into
the email editor's per-deal "Logo URL" field. This logos/ folder can be deleted.
